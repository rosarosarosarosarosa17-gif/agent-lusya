// Единая конфигурация агента.
// Раздел 3: часть значений переедет в .env, но структура останется той же
export default {
  name: 'Мой агент v1',            // имя агента
  tickMs: 5000,                    // интервал тика в миллисекундах
  model: 'gpt-4o-mini',           // AI-модель (заглушка, заменим позже)
  commands: ['/помощь', '/статус'], // команды, которые агент умеет принимать
};
